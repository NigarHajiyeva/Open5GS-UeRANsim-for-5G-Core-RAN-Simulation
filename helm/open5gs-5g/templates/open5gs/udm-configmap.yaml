{{- if .Values.open5gs.udm.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: udm-config
  namespace: {{ .Values.global.namespace }}
data:
  udm.yaml: |
    logger:
      level: info

    udm:
      sbi:
        server:
          - address: 0.0.0.0
            port: 7777
            advertise: {{ include "open5gs-5g.serviceFQDN" (dict "name" "udm" "root" .) }}
        client:
          nrf:
            - uri: http://{{ include "open5gs-5g.serviceFQDN" (dict "name" "nrf" "root" .) }}:7777
      metrics:
        server:
          - address: 0.0.0.0
            port: 9090
{{- end }}
